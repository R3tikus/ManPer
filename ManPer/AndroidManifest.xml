<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamper"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />
    <!-- PERMISOS REQUERIDOS -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- LIBRERIAS -->
        <uses-library android:name="com.google.android.maps" />
        <!-- ACTIVIDAD SPLASH -->
        <activity
            android:name="com.mamper.SplashActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- ACTIVIDAD PRINCIPAL -->
        <activity
            android:name="Principal"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
        </activity>
        <!-- ACTIVIDAD EDITPLACE -->
        <activity
            android:name="com.mamper.EditarLugarActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
        </activity>
        <!-- ACTIVIDAD LUGARES ACTIVITY -->
        <activity
            android:name="ListaLugaresActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
>
        </activity>

        <!-- ACTIVIDAD MAPA LUGARES -->
        <activity
            android:name="MapaLugaresActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
>
        </activity>

        <!-- ACTIVIDAD MOSTRAR LUGARES -->
        <activity
            android:name="MostrarLugarActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
>
        </activity>
        <activity android:name="Languages"></activity>
    </application>

</manifest>